import 'package:flutter/material.dart';

class AppColors {
  // Background colors
  static const Color backgroundWhite = Colors.white;
  static const Color backgroundDark = Color(0xff121212); // Darker for better contrast

  // Container colors
  static const Color containerLight = Colors.white;
  static const Color containerDark = Color(0xff1E1E1E); // Slightly lighter than background

  // Text colors
  static const Color textLight = Colors.black;
  static const Color textDark = Colors.white;

  // Primary colors (now using BlueAccent variants)
  static const Color primaryLight = Colors.blueAccent;
  static const Color primaryDark = Colors.blueAccent;

  // Secondary colors
  static const Color secondaryLight = Color(0xff82B1FF); // Lighter blue
  static const Color secondaryDark = Color(0xff2962FF); // Darker blue

  // Accent colors
  static const Color red = Color(0xffF44336);
  static const Color orange = Color(0xffFF9800);

  // Timer text color
  static const Color timerTextLight = Colors.black;
  static const Color timerTextDark = Colors.white;

  // Button colors
  static const Color buttonLight = Colors.blueAccent;
  static const Color buttonDark = Colors.blueAccent;
}
